using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Person : MonoBehaviour
{
    /*public float maxUpAndDown = .2f;             // amount of meters going up and down
    public float speed = 50;            // up and down speed
    public float angle = 0;            // angle to determin the height by using the sinus
    public float toDegrees = Mathf.PI / 180;    // radians to degrees*/

    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {
     /*   angle += speed * Time.deltaTime;
        if (angle > 360) angle -= 360;
        var y = maxUpAndDown * Mathf.Sin(angle * toDegrees);
        transform.position = new Vector3(transform.position.x, y, transform.position.z);*/

    }
}
